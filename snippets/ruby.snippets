snippet cn
	module ${1:`substitute(vim_snippets#Filename(), "\(_\|^\)\(.\)", "\u\2", "g")`}
		extend ActiveSupport::Concern

		${0}
	end

snippet int
	module ${1:`substitute(expand('%:p:h:t'), '\(_\|^\)\(.\)', '\u\2', 'g')`}
		class ${2:`substitute(vim_snippets#Filename(), '\(_\|^\)\(.\)', '\u\2', 'g')`} < ActiveInteraction::Base
			def execute
				${0}
			end
		end
	end

snippet sint
	require "rails_helper"

	describe ${1:`snippets#interaction_spec_name()`} do
		describe ".run" do
			subject { described_class.run! }

			${0}
		end
	end

snippet letdo
	let(:${1:object}) do
		${0}
	end

snippet letdo!
	let!(:${1:object}) do
		${0}
	end

snippet subjdo
	subject(:${1:object}) do
		${0}
	end
